[variables]
NIXPACKS_NODE_VERSION = "20.19.0"

[phases.install]
cmds = ["corepack enable", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm build"]

[start]
cmd = "pnpm preview --host 0.0.0.0 --port ${PORT:-3000}"